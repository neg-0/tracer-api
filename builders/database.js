/**
 * Initializes all files needed for a database service.
 * @param {string} cwd 
 * @param {{}} service 
 */
function init(projectPath, servicePath, service) {
  // Nothing is required here as the database will be created by the docker compose file
}

module.exports = {
  init
};